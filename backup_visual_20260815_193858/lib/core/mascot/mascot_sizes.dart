/// Escala de tamanho da Lili Fit — presença visual premium.
class MascotSizes {
  MascotSizes._();

  /// Elementos secundários (chips, listas).
  static const double small = 96;

  /// Avatar no app bar / lista.
  static const double avatar = 96;

  /// Header da Home (ao lado do cumprimento).
  static const double header = 128;

  /// Cards, mensagens e dicas.
  static const double medium = 200;

  /// Card motivacional.
  static const double heroCard = 220;

  /// Guia / balão de fala.
  static const double guide = 210;

  /// Empty state / splash.
  static const double large = 280;

  static const double showcase = 300;

  /// Auth / onboarding.
  static const double auth = 260;

  /// Limites do modo hero responsivo (~35–50% da tela).
  static const double heroMin = 200;
  static const double heroMax = 360;
  static const double showcaseMin = 240;
  static const double showcaseMax = 420;
}
